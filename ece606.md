# ECE 606 Lecture Notes

## Lecture 1: Sep 9, 2026

- Algorithm: A step-by-step procedure to compute a function.
- Set: A collection of distinct items with no ordering assumed / composed. e.g., $\{20, 10, 30\}$ is a set but $\{20, 10, 30, 10\}$ is not a set because it contains a duplicate, it is a multiset.
- Function: $f: A \to B$, where $A$ is the domain and $B$ is the codomain, where each $a \in A$ is associated with a single item $b \in B$: $f(a) = b$.
- Given sets $A, B$, $A \setminus B = \{x \in A \mid x \notin B\}$. e.g., $\{1, 2, 3\} \setminus \{2, 3, 4\} = \{1\}$.
- If $x$ and $y$ are each propositions, then so are $x \land y$, $x \lor y$, $\neg x$, $x \implies y$, $\cdots$

  Note that $\neg x$ and $x \implies y$ follow the following truth tables:

  | $x$ | $\neg x$ |
  | --- | --- |
  | 0 | 1 |
  | 1 | 0 |

  | $x$ | $y$ | $x \implies y$ |
  | --- | --- | --- |
  | 0 | 0 | 1 |
  | 0 | 1 | 1 |
  | 1 | 0 | 0 |
  | 1 | 1 | 1 |

- Example: True or False? If I am not the pope implies that I am the pope, then I am the pope.

  > Let $p$ be the proposition "I am not the pope". Then the statement can be rewritten as $(p \implies \neg p) \implies \neg p$.
  >
  > Now, the truth table is as follows:
  >
  > | $p$ | $\neg p$ | $p \implies \neg p$ | $(p \implies \neg p) \implies \neg p$ |
  > | --- | --- | --- | --- |
  > | 0 | 1 | 1 | 1 |
  > | 1 | 0 | 0 | 1 |
  >
  > Therefore, the statement is a tautology, and the answer is True.

## Lecture 2: Sep 14, 2026

- Example: Prove that no positive integer $x,y$ exists such that $x^2-y^2=1$.

  > Suppose there exist positive integers $x,y$ such that $x^2-y^2=1$. Then, $(x+y)(x-y)=1$. Since $x$ and $y$ are positive integers, $x+y \geq 2$ and $x-y \geq 1$. Therefore, $(x+y)(x-y) \geq 2 \cdot 1 = 2$, which contradicts the assumption that $(x+y)(x-y)=1$. Hence, no such positive integers $x,y$ exist.

  By contradiction: For the purpose of contradiction, assume $x,y \in \mathbb{Z}^+$ such that $x^2-y^2=1$. Now, $x^2-y^2=(x+y)(x-y)=1$. Seek to prove $x,y \in \mathbb{Z}^+ \implies (x+y)(x-y) \neq 1$.

  Either (1) $x+y=1$ and $x-y=1$ or (2) $x+y=-1$ and $x-y=-1$.

  Case (1): $x+y=1$ and $x-y=1$. Then, $2x=2 \implies x=1$. Then, $y=0$, which is not a positive integer. Contradiction.

  Case (2): $x+y=-1$ and $x-y=-1$. Then, $2x=-2 \implies x=-1$. Then, $y=0$, which is not a positive integer. Contradiction.

- Example: for all positive integers $n$:

  $$
  \sum_{i=1}^{n} i^3 = \left( \sum_{i=1}^{n} i \right)^2
  $$

  > By induction on $n$:
  >
  > Base case: $n=1$. Then, $\sum_{i=1}^{1} i^3 = 1^3 = 1$ and $\left( \sum_{i=1}^{1} i \right)^2 = 1^2 = 1$.
  >
  > Inductive step:
  >
  > Use the following identity:
  >
  > $$
  > \sum_{i=1}^{n} i = \frac{n(n+1)}{2}
  > $$
  >
  > Proof: By induction on $n$:
  >
  > Base case: $n=1$. Then, $\sum_{i=1}^{1} i = 1 = \frac{1(1+1)}{2}$.
  >
  > Inductive step: Assume $\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$. Then, $\sum_{i=1}^{n+1} i = \sum_{i=1}^{n} i + (n+1) = \frac{n(n+1)}{2} + (n+1) = \frac{n(n+1)+2(n+1)}{2} = \frac{(n+1)(n+2)}{2}$.
  >
  > Exercise: Prove the original statement by induction on $n$ using the identity above.

- Factorial: For $n \in \mathbb{Z}^+$,

  $$
  n! = \begin{cases} 1 & n=1 \\ n(n-1)! & n>1 \end{cases}
  $$

- Example: How many different rearrangements of letters do we have of "bookkeeper"?

  > $\frac{10!}{1!2!2!3!1!1!} = 151200$ different rearrangements.

- Example: Given $S = \{1, 2, \cdots, n\}$ where $n \in \mathbb{Z}^+$, how many subsets of $S$ are there?

  > The subset of $S = 2^n = \binom{n}{0} + \binom{n}{1} + \cdots + \binom{n}{n}$.

- Experiment + Outcome is elementary event. Event is the a subset of elementary events, is a subset of the sample space. Sample space is the set of all elementary events.

- $Pr(A \mid B) = \frac{Pr(A \cap B)}{Pr(B)}$ if $Pr(B) > 0$.

- Example: A bag contains 4 red items and 2 blue items. Alice reaches in and takes, sequentially without replacement, two items. What is the probability that the first item Alice draws is red given that the second item Alice draws is blue?

  > Define the following events:
  >
  > - $A$: the first item Alice draws is red.
  > - $B$: the second item Alice draws is blue.
  >
  > Sample space: $\{RR, RB, BR, BB\}$.
  >
  > $A \cap B = \{RB\}$, $B = \{RB, BB\}$.
  >
  > $Pr(A \cap B) = \frac{4}{15}$, $Pr(B) = \frac{1}{3}$.
  >
  > Therefore, $Pr(A \mid B) = \frac{Pr(A \cap B)}{Pr(B)} = \frac{4/15}{1/3} = \frac{4}{5}$.

- Independent: The events $A$ and $B$ are independent if $Pr(A \mid B) = Pr(A)$, which is equivalent to $Pr(A \cap B) = Pr(A)Pr(B)$.

- Example: We toss two fair 6-sided dice. What do we expect the sum the two be?

  > Method 1:
  >
  > | Elem. event | Probability |
  > | ---: | ---: |
  > | 2 | $\frac{1}{36}$ |
  > | 3 | $\frac{2}{36}$ |
  > | 4 | $\frac{3}{36}$ |
  > | $\vdots$ | $\vdots$ |
  > | 12 | $\frac{1}{36}$ |
  >
  > If $X$ is a random variable that is the sum of two dice:
  >
  > $$
  > E[X]=\frac{1}{36}\times 2+\frac{2}{36}\times 3+\frac{3}{36}\times 4+\cdots+\frac{1}{36}\times 12
  > $$
  >
  > Method 2: Let $X_1$ be the value of the first die and $X_2$ be the value of the second die. Then, $X = X_1 + X_2$. By linearity of expectation:
  >
  > $$
  > E[X] = E[X_1 + X_2] = E[X_1] + E[X_2]
  > $$
  >
  > $E[X_1] = E[X_2] = \frac{1+2+3+4+5+6}{6} = 3.5$. Therefore, $E[X] = 3.5 + 3.5 = 7$.

## Lecture 3: Sep 16, 2026

- Data structure:

  - Array:
    - Sqeuqnce
    - Indexed (`A[i]`)
    - Fixed size (`A[1,...,n]`)
  - Set:
    - Unordered
    - Distinct
    - Ex:

      ```text
      Set_Add(S,i):
          S <- S ∪ {i}
      ```

- Graph (directed): is an ordered pair $\langle V,E \rangle$ of sets, where

  - $V$ is a set
  - $E \subseteq V \times V$
  - A graph $\langle V,E \rangle$ is said to be non-empty if $V \neq \emptyset$.
  - A graph $\langle V,E \rangle$ is finite if $V$ is finite.

- Ordered pair $\langle x,y \rangle$ is

  $$
  \langle x,y \rangle = \begin{cases} \{x, \{x\}\} & x=y \\ \{x, \{x,y\}\} & x \neq y \end{cases}
  $$

  - Claim: Given ordered pairs $\langle x,y \rangle$ and $\langle u,v \rangle$, then $\langle x,y \rangle = \langle u,v \rangle \iff x=u$ and $y=v$.

- Undirected graph: is an ordered pair $\langle V,E \rangle$ of sets, where

  - $V$ is a set
  - $E = \{\{x,y\} \mid x,y \in V, x \neq y\}$.

- Path: A path in a graph $<V,E>$ is a sequence $<u_1, u_2, \cdots, u_n>$ where each $u_i \in V$ and $\{u_{i-1}, u_i\} \in E$ for $i=1,2,\cdots,k$.
    - Such a path is of length $k$ (number of edges in the path).
    - Given $u \in V$, we admit a 0-length path $<u>$.
    - We say $u \in V$ is reachable from $v \in V$ if there exists a path from $v$ to $u$ in $G$.
    - A path $<u_0, u_1, \cdots, u_k>$ is a cycle if $u_0 = u_k$.
    - Subpath: A path $<u_i, u_{i+1}, \cdots, u_j>$ is a subpath of $<u_0, u_1, \cdots, u_k>$ if $0 \leq i < j \leq k$.
    
- Graph (continue...)
    - An undirected graph $G = <V,E>$ is connected if every vertex in $V$ is reachable from every other vertex.
    - A graph is acyclic if it contains no cycles.
    - Given a graph $G = <V,E>$, a subgraph of $G$ is a graph $H = <V',E'>$ such that $V' \subseteq V$ and $E' \subseteq E$.
    - A subset $G' = <S, E'>$ of a graph $G = <V,E>$ is induced by $S \subseteq V$ if it has edges $E' = \{\{x,y\} \in E \mid x,y \in S\}$.